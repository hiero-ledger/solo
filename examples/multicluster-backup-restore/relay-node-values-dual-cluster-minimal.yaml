# Minimal dual-cluster overrides for the JSON-RPC relay.
#
# The default 250Mi limit is intermittently insufficient while the relay initializes
# against the restored mirror node database in CI.
resources:
  limits:
    memory: 500Mi
