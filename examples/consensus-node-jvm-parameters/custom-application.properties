# Custom application.properties — additive override example.
# These entries are appended onto Solo's default application.properties.
# Duplicate keys will override the Solo default; new keys will be added.
hedera.recordStream.logPeriod=2
