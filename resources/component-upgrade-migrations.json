{
  "components": {
    "block-node": {
      "defaultStrategy": "in-place",
      "boundaries": [
        {
          "version": "0.28.1",
          "strategy": "recreate",
          "reason": "The 0.28.1 chart introduced blockNode.persistence.plugins; --reuse-values from 0.28.0 fails with a nil pointer error. StatefulSet must be recreated with fresh values."
        },
        {
          "version": "0.32.0",
          "strategy": "in-place",
          "reason": "The 0.32.0 chart moved metrics from blockNode.health.metrics to blockNode.metrics; --reuse-values from 0.31.x can miss required keys and fail template rendering.",
          "extraCommandArgs": [
            "--set",
            "blockNode.metrics.hostname=0.0.0.0",
            "--set",
            "blockNode.metrics.port=16007",
            "--set",
            "blockNode.metrics.path=/metrics"
          ]
        },
        {
          "version": "0.37.0",
          "strategy": "recreate",
          "reason": "The 0.37.0 chart adds blockNode.persistence.applicationState and changes StatefulSet volumeClaimTemplates, which are immutable and require recreating the StatefulSet."
        },
        {
          "version": "0.38.1",
          "strategy": "recreate",
          "reason": "BN v0.38.1 in-place upgrade from 0.37.0 inherits a stale block-ranges.json application-state whose recorded block range can be ahead of disk (block opened but not written before publisher disconnect). This causes ServerStatus to report an incorrect first_available_block, creating a permanent streaming deadlock with CN v0.75 after a FreezeUpgrade. Recreate ensures clean state. See hiero-block-node#3249."
        }
      ]
    }
  }
}
