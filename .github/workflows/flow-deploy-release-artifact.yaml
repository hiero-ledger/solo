##
# Copyright (C) 2023-2024 Hedera Hashgraph, LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
##

name: "Deploy Release Artifact"
on:
  workflow_dispatch:
    inputs:
      dry-run-enabled:
        description: "Perform Dry Run"
        type: boolean
        required: false
        default: true
      dual-publish-enabled:
        description: "Dual Publish Enabled"
        type: boolean
        required: false
        default: true

env:
  REGISTRY: ghcr.io
  HASHGRAPH_PKG_NAME: "@hashgraph/solo"
  HIERO_PKG_NAME: "@hiero-ledger/solo"

defaults:
  run:
    shell: bash

permissions:
  contents: write
  packages: write
  pages: write
  id-token: write
  issues: write
  pull-requests: write

jobs:
  prepare-release:
    name: Prepare Workflow Run
    runs-on: hiero-solo-linux-medium
    outputs:
      version: ${{ steps.set-outputs.outputs.version }}
      need-hg-release: ${{ steps.set-outputs.outputs.need-hg-release }}
      need-hl-release: ${{ steps.check-hl-release-needed.outputs.need-hl-release || 'false' }}
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@a90bcbc6539c36a85cdfeb73f7e2f433735f215b # v2.15.0
        with:
          egress-policy: audit

      - name: Prepare Release
        uses: pandaswhocode/initialize-github-job@a57cd6d8d768b2f3c95334bdd4fa8c21609fc651 # 1.0.5
        with:
          checkout: 'true'
          checkout-ref: '${{ github.ref }}'
          checkout-token: '${{ secrets.GH_ACCESS_TOKEN }}'
          setup-node: 'true'
          node-version: '22'
          node-registry: 'https://registry.npmjs.org/'

      - name: Setup JQ
        uses: dcarbone/install-jq-action@b7ef57d46ece78760b4019dbc4080a1ba2a40b45 # v3.2.0
        with:
          version: 1.7

      - name: Install Semantic Release
        run: |
          npm install -g semantic-release@25.0.2 @semantic-release/git@10.0.1 @semantic-release/github@12.0.2 @semantic-release/exec@7.1.0 gradle-semantic-release-plugin@1.7.6
          npm install -g conventional-changelog-conventionalcommits@9.1.0 @commitlint/cli@18.6.0 @commitlint/config-conventional@18.6.0
          npm install -g marked-mangle@1.1.6 marked-gfm-heading-id@3.1.2 semantic-release-conventional-commits@3.0.0

      - name: Calculate Next Version (default branch)
        id: calculate-version
        env:
          GITHUB_TOKEN: ${{ secrets.GH_ACCESS_TOKEN }}
          GIT_AUTHOR_NAME: ${{ secrets.GIT_USER_NAME }}
          GIT_AUTHOR_EMAIL: ${{ secrets.GIT_USER_EMAIL }}
          GIT_COMMITTER_NAME: ${{ secrets.GIT_USER_NAME }}
          GIT_COMMITTER_EMAIL: ${{ secrets.GIT_USER_EMAIL }}
        run: |
          PROCEED="true"
          if [[ "${{ github.event.repository.default_branch }}" == "${{ github.ref_name }}" ]]; then
            if SEMREL_OUTPUT=$(npx semantic-release --dry-run 2>&1); then
              if echo "${SEMREL_OUTPUT}" | grep -q "The next release version is"; then
                echo "::notice file=flow-deploy-release-artifact.yaml::Semantic Release detected a new version"
              else
                echo "::notice file=flow-deploy-release-artifact.yaml::Semantic Release detected NO new version"
                PROCEED="false"
              fi
            else
              echo "::notice file=flow-deploy-release-artifact.yaml::Semantic Release did NOT run successfully"
              PROCEED="false"
            fi
            echo "::group::SemVer output"
            echo "${SEMREL_OUTPUT}"
            echo "::endgroup::"
          else
            if [[ "${{ inputs.dry-run-enabled }}" == "true" ]]; then
              jq -r '.version' './package.json' > VERSION
              PROCEED="true"
            else
              PROCEED="false"
            fi
          fi

          echo "need-release=${PROCEED}" >> "${GITHUB_OUTPUT}"

      - name: Extract Version
        id: set-outputs
        if: ${{ !cancelled() && always() }}
        run: |
          PROCEED="true"
          OUTVER=""
          
          if [[ "${{ steps.calculate-version.outputs.need-release }}" == "true" ]] && [[ -f VERSION ]]; then
            cat VERSION
            TRIMMED_VERSION=$(cat VERSION | tr -d '[:space:]')
            OUTVER="${TRIMMED_VERSION}"
            echo "version=${TRIMMED_VERSION}"
          elif [[ "${{ steps.calculate-version.outputs.need-release }}" == "true" ]] && [[ "${{ inputs.dry-run-enabled }}" == "true" ]] && [[ ! -f VERSION ]]; then
            VER=$(jq -r '.version' './package.json')
            OUTVER="${VER}"
            echo "Dry run enabled and VERSION file not found, using package.json version: ${VER}"
          else
            PROCEED="false"
          fi

          echo "version=${OUTVER}" >> "${GITHUB_OUTPUT}"
          echo "need-hg-release=${PROCEED}" >> "${GITHUB_OUTPUT}"

      - name: "Check: Hiero-Ledger Release Needed"
        id: check-hl-release-needed
        if: ${{ steps.calculate-version.outputs.need-release == 'true' && inputs.dual-publish-enabled == true }}
        run: |
          PUBLISH_REQUIRED="false"
          if ! curl -sSLf "https://registry.npmjs.org/${{ env.HIERO_PKG_NAME }}/${{ steps.set-outputs.outputs.version }}" >/dev/null 2>&1; then
            PUBLISH_REQUIRED="true"
          fi
          
          echo "need-hl-release=${PUBLISH_REQUIRED}" >> "${GITHUB_OUTPUT}"

  get-package-info:
    name: 'Get Package Info'
    runs-on: hiero-solo-linux-medium
    if: ${{ needs.prepare-release.outputs.need-hg-release == 'true' || needs.prepare-release.outputs.need-hl-release == 'true' }}
    needs:
      - prepare-release
    outputs:
      hg-package-name: ${{ steps.set-hg-package-name.outputs.hg-package-name }}
      hl-package-name: ${{ steps.set-hl-package-name.outputs.hl-package-name }}
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@a90bcbc6539c36a85cdfeb73f7e2f433735f215b # v2.15.0
        with:
          egress-policy: audit

      - name: Prepare Job
        uses: pandaswhocode/initialize-github-job@a57cd6d8d768b2f3c95334bdd4fa8c21609fc651 # 1.0.5
        with:
          checkout: 'true'
          checkout-ref: '${{ github.ref }}'
          checkout-token: '${{ secrets.GITHUB_TOKEN }}'
          checkout-fetch-depth: '1'

      - name: Setup JQ
        uses: dcarbone/install-jq-action@b7ef57d46ece78760b4019dbc4080a1ba2a40b45 # v3.2.0
        with:
          version: 1.7

      - name: Generate Hashgraph Package Name
        id: set-hg-package-name
        run: |
          clean_pkg_name() {
            echo "${1}" | sed 's/^@//; s/\//-/g'
          }
          pname=$(jq -r '.name' './package.json')
          if [[ "${pname}" != "${{ env.HASHGRAPH_PKG_NAME }}" ]]; then
            echo "::error file=flow-deploy-release-artifact.yaml::Package name in package.json does not match expected package name '${{ env.HASHGRAPH_PKG_NAME }}'"
            exit 1
          fi
          full_pname=$(clean_pkg_name "${pname}-${{ needs.prepare-release.outputs.version }}.tgz")
          echo "hg-package-name=${full_pname}" | tee -a "${GITHUB_OUTPUT}"

      - name: Generate Hiero-Ledger Package Name
        id: set-hl-package-name
        if: ${{ inputs.dual-publish-enabled == true }}
        run: |
          clean_pkg_name() {
            echo "${1}" | sed 's/^@//; s/\//-/g'
          }
          pname="${{ env.HIERO_PKG_NAME }}"
          full_pname=$(clean_pkg_name "${pname}-${{ needs.prepare-release.outputs.version }}.tgz")
          echo "hl-package-name=${full_pname}" | tee -a "${GITHUB_OUTPUT}"

  create-github-release:
    name: 'Create GitHub Release'
    runs-on: hiero-solo-linux-medium
    if: ${{ needs.prepare-release.outputs.need-hg-release == 'true' || needs.prepare-release.outputs.need-hl-release == 'true' }}
    needs:
      - prepare-release
      - get-package-info
    outputs:
      need-hg-publish: ${{ steps.verify-package.outputs.need-hg-publish }}
      need-hl-publish: ${{ steps.update-for-dual-publish.outputs.need-hl-publish || 'false' }}
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@a90bcbc6539c36a85cdfeb73f7e2f433735f215b # v2.15.0
        with:
          egress-policy: audit

      - name: Prepare Job
        uses: pandaswhocode/initialize-github-job@a57cd6d8d768b2f3c95334bdd4fa8c21609fc651 # 1.0.5
        with:
          checkout: 'true'
          checkout-ref: '${{ github.ref }}'
          checkout-token: '${{ secrets.GH_ACCESS_TOKEN }}'
          setup-node: 'true'
          node-version: '22'
          node-registry: 'https://registry.npmjs.org/'

      - name: Install GnuPG Tools
        run: |
          if ! command -v gpg2 >/dev/null 2>&1; then
            echo "::group::Configure APT Mirror"
              # Configure APT to use main Ubuntu archive (disable mirror rotation)
              sudo sed -i 's|http://[a-z.]*.ubuntu.com|http://archive.ubuntu.com|g' /etc/apt/sources.list
            echo "::endgroup::"
            echo "::group::Updating APT Repository Indices"
              sudo apt update
            echo "::endgroup::"
            echo "::group::Installing GnuPG Tools"
              sudo apt install -y gnupg2
            echo "::endgroup::"
          fi

      - name: Import GPG key
        id: gpg_key
        uses: step-security/ghaction-import-gpg@69c854a83c7f79463f8bdf46772ab09826c560cd # v6.3.1
        with:
          gpg_private_key: ${{ secrets.GPG_KEY_CONTENTS }}
          passphrase: ${{ secrets.GPG_KEY_PASSPHRASE }}
          git_config_global: true
          git_user_signingkey: true
          git_commit_gpgsign: true
          git_tag_gpgsign: false

      - name: Install Task
        uses: arduino/setup-task@b91d5d2c96a56797b48ac1e0e89220bf64044611 # v2.0.0
        with:
          version: 3.39.2
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup JQ
        uses: dcarbone/install-jq-action@b7ef57d46ece78760b4019dbc4080a1ba2a40b45 # v3.2.0
        with:
          version: 1.7

      - name: Build Production Distribution
        run: |
          set -eo pipefail
          npm install -g npm@11.7.0
          npm ci
          echo "VERSION=${{ needs.prepare-release.outputs.version }}"
          if [[ "${{ inputs.dry-run-enabled }}" == "true" ]]; then
            [[ -n "${{ needs.prepare-release.outputs.version }}" ]] && npm version ${{ needs.prepare-release.outputs.version }} -f --no-git-tag-version --allow-same-version
          fi
          task build

      - name: Upload Logs
        if: ${{ always() }}
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          path: /home/runner/.npm/_logs

      - name: Setup JFrog CLI
        uses: jfrog/setup-jfrog-cli@279b1f629f43dd5bc658d8361ac4802a7ef8d2d5 # v4.9.1
        env:
          JF_URL: ${{ vars.JF_URL }}
          JF_ACCESS_TOKEN: ${{ secrets.JF_ACCESS_TOKEN }}

      - name: Show JFrog Config
        run: jfrog config show

      - name: Verify JFrog Connectivity
        run: jfrog rt ping

      - name: Setup JFrog NPM Repository
        run: |
          jf npmc --server-id-resolve setup-jfrog-cli-server --server-id-deploy setup-jfrog-cli-server --repo-resolve solo-npm-release --repo-deploy solo-npm-release
          echo "::group::JFrog NPM Repository Configuration"
            cat .jfrog/projects/npm.yaml
          echo "::endgroup::"

      - name: Install Semantic Release
        run: |
          npm install -g semantic-release@25.0.2 @semantic-release/git@10.0.1 @semantic-release/github@12.0.2 @semantic-release/exec@7.1.0 gradle-semantic-release-plugin@1.7.6
          npm install -g conventional-changelog-conventionalcommits@9.1.0 @commitlint/cli@18.6.0 @commitlint/config-conventional@18.6.0
          npm install -g marked-mangle@1.1.6 marked-gfm-heading-id@3.1.2 semantic-release-conventional-commits@3.0.0

      - name: Install Dependencies
        run: jf npm ci

      - name: Set Publish Data
        id: set-publish-data
        run: |
          VERSION="${{ needs.prepare-release.outputs.version }}"
          SOLO_NPM_PACKAGE_NAME="hashgraph-solo-${VERSION}.tgz"
          echo "artifact-name=${SOLO_NPM_PACKAGE_NAME}" >> $GITHUB_OUTPUT

      - name: Publish Semantic Release
        env:
          GH_TOKEN: ${{ secrets.GH_ACCESS_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GH_ACCESS_TOKEN }}
          GIT_AUTHOR_NAME: ${{ secrets.GIT_USER_NAME }}
          GIT_AUTHOR_EMAIL: ${{ secrets.GIT_USER_EMAIL }}
          GIT_COMMITTER_NAME: ${{ secrets.GIT_USER_NAME }}
          GIT_COMMITTER_EMAIL: ${{ secrets.GIT_USER_EMAIL }}
        if: ${{ needs.prepare-release.outputs.need-hg-release == 'true' && !cancelled() && !failure() }}
        run: |
          FLAGS=""
          if [[ "${{ inputs.dry-run-enabled }}" == "true" ]]; then
            FLAGS="--dry-run"
          fi

          npx semantic-release --no-sign ${FLAGS}

          if [[ -d "dist" ]]; then
            ls -l dist
          fi

      - name: Rebuild dist after release version bump
        if: ${{ github.event.inputs.dry-run-enabled != true && !cancelled() && !failure() }}
        run: |
          # task build may be considered up-to-date in this job, so sync version explicitly
          cp package.json dist/package.json
          echo "root package version: $(node -p \"require('./package.json').version\")"
          echo "dist package version: $(node -p \"require('./dist/package.json').version\")"

      - name: Deploy to JFrog Registry
        if: ${{ needs.prepare-release.outputs.need-hg-release == 'true' && !cancelled() && !failure() }}
        run: |
          if [[ "${{ inputs.dry-run-enabled }}" == "true" ]]; then
            echo "Dry run enabled, skipping publish to JFrog registry."
          else
            jf npm publish
          fi

      - name: Create Example Asset Archives and Upload to Release
        if: ${{ github.event.inputs.dry-run-enabled != true && !cancelled() && !failure() }}
        run: |
          echo "Creating example asset archives for release..."
          GH_TOKEN=${{ secrets.GITHUB_TOKEN }} task -t scripts/Taskfile.examples.yml create-example-asset-archives UPLOAD_TO_RELEASE=true RELEASE_TAG=v${{ needs.prepare-release.outputs.version }} CLEAN_UP=true
          echo "Example assets created and uploaded to release."

      - name: Create Hashgraph NPM Package
        if: ${{ needs.prepare-release.outputs.need-hg-release == 'true' }}
        run: |
          echo "::group::View Version to publish to npm"
            echo "Version to publish:"
            cat package.json | grep 'version'
          echo "::endgroup::"

          echo "::group::Build NPM Package"
          task build
          echo "::endgroup::"

          echo "::group::Create Package"
            FLAGS=""
            if [[ "${{ inputs.dry-run-enabled }}" == "true" ]]; then
              echo "Command: npm pack --pack-destination \"output/hg\""
              FLAGS="--dry-run"
            fi
            mkdir -p "output/hg"
            npm pack --pack-destination "output/hg" ${FLAGS}
          echo "::endgroup::"

      - name: Validate Hashgraph package version consistency
        if: ${{ needs.prepare-release.outputs.need-hg-release == 'true' }}
        run: |
          bash scripts/verify-package-version-consistency.sh

      - name: Verify Hashgraph Package is Created
        id: verify-package
        if: ${{ needs.prepare-release.outputs.need-hg-release == 'true' }}
        run: |
          PROCEED="true"
          echo "::group::Verify Package is Created"
            if ls -l output/hg | grep "${{ needs.get-package-info.outputs.hg-package-name }}" > /dev/null 2>&1 ; then
              echo "::notice file=flow-deploy-release-artifact.yaml::Hashgraph Package is created"
            else
              PROCEED="false"
              echo "::error file=flow-deploy-release-artifact.yaml::Hashgraph Package is not created"
              echo "Attempt Manual Publish!"
            fi
          echo "::endgroup::"
          echo "need-hg-publish=${PROCEED}" >> "${GITHUB_OUTPUT}"

      - name: Upload Hashgraph Package
        if: ${{ needs.prepare-release.outputs.need-hg-release == 'true' && steps.verify-package.outputs.need-hg-publish == 'true' }}
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: npm-hg-package
          path: output/hg
          if-no-files-found: error

      - name: Update Files for Dual Publish
        id: update-for-dual-publish
        if: ${{ inputs.dual-publish-enabled == true && (needs.prepare-release.outputs.need-hl-release == 'true' || inputs.dry-run-enabled == true) }}
        env:
          IGNORE_LIST_FILE: .github/workflows/support/.ignore-deploy-release
          CI_TASK: scripts/Taskfile.release.yml
        run: |
          echo "::group::Rename package references"
          task -t ${{ env.CI_TASK }} dual-publish:rename \
            HASHGRAPH_PKG_NAME="${{ env.HASHGRAPH_PKG_NAME }}" \
            HIERO_PKG_NAME="${{ env.HIERO_PKG_NAME }}" \
            IGNORE_LIST_FILE="${{ env.IGNORE_LIST_FILE }}"
          echo "::endgroup::"

          echo "::group::Build and pack Hiero-Ledger package"
          task -t ${{ env.CI_TASK }} dual-publish:pack \
            OUTPUT_DIR="output/hl" \
            DRY_RUN="${{ inputs.dry-run-enabled }}"
          echo "::endgroup::"

          PROCEED="true"
          echo "::group::Verify Hiero-Ledger Package is Created"
          if ls -l output/hl | grep "${{ needs.get-package-info.outputs.hl-package-name }}" > /dev/null 2>&1; then
            echo "::notice file=flow-deploy-release-artifact.yaml::Hiero-Ledger Package is created"
          else
            PROCEED="false"
            echo "::error file=flow-deploy-release-artifact.yaml::Hiero-Ledger Package is not created"
            echo "Attempt Manual Publish!"
          fi
          echo "::endgroup::"

          echo "need-hl-publish=${PROCEED}" >> "${GITHUB_OUTPUT}"

          echo "::group::Version to publish"
          grep '"version"' package.json
          echo "::endgroup::"

      - name: Validate Hiero-Ledger package version consistency
        if: ${{ inputs.dual-publish-enabled == true && needs.prepare-release.outputs.need-hl-release == 'true' && inputs.dry-run-enabled != true }}
        run: |
          bash scripts/verify-package-version-consistency.sh "output/hl/*.tgz"

      - name: Upload Hiero-Ledger Package
        if: ${{ inputs.dual-publish-enabled == true && needs.prepare-release.outputs.need-hl-release == 'true' && steps.update-for-dual-publish.outputs.need-hl-publish == 'true' }}
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: npm-hl-package
          path: output/hl
          if-no-files-found: error

  publish-npm-packages:
    name: Publish Release Packages
    runs-on: ubuntu-latest
    needs:
      - get-package-info
      - create-github-release
    if: ${{ needs.create-github-release.outputs.need-hg-publish == 'true' || needs.create-github-release.outputs.need-hl-publish == 'true' || inputs.dry-run-enabled == true }}
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@a90bcbc6539c36a85cdfeb73f7e2f433735f215b # v2.15.0
        with:
          egress-policy: audit

      - name: Prepare Job
        uses: pandaswhocode/initialize-github-job@a57cd6d8d768b2f3c95334bdd4fa8c21609fc651 # 1.0.5
        with:
          checkout: 'true'
          checkout-ref: '${{ github.ref }}'
          checkout-token: '${{ secrets.GITHUB_TOKEN }}'
          setup-node: 'true'
          node-version: '22'
          node-registry: 'https://registry.npmjs.org/'

      - name: Install NPM latest
        run: npm install -g npm@11.7.0

      - name: Download Hashgraph NPM Package Artifact
        if: ${{ needs.create-github-release.outputs.need-hg-publish == 'true' && inputs.dry-run-enabled != true }}
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: npm-hg-package
          path: output/hg

      - name: Download Hiero-Ledger NPM Package Artifact
        if: ${{ needs.create-github-release.outputs.need-hl-publish == 'true' && inputs.dry-run-enabled != true }}
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: npm-hl-package
          path: output/hl

      - name: Install Task
        uses: arduino/setup-task@b91d5d2c96a56797b48ac1e0e89220bf64044611 # v2.0.0
        with:
          version: 3.39.2
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish Hashgraph NPM Package
        if: ${{ inputs.dry-run-enabled == true || needs.create-github-release.outputs.need-hg-publish == 'true' }}
        run: |
          echo "::group::Set Publish Args"
            FLAGS="--access=public"
            echo "flags=${FLAGS}"
          echo "::endgroup::"

          echo "::group::Set tarball output"
            TARBALL="./output/hg/${{ needs.get-package-info.outputs.hg-package-name }}"
            if [[ "${{ inputs.dry-run-enabled }}" == "true" ]]; then
              TARBALL=""
            fi
            echo "tarball=${TARBALL}"
          echo "::endgroup::"

          echo "::group::Publish NPM Package"
            if [[ "${{ inputs.dry-run-enabled }}" != "true" ]]; then
              npm publish ${TARBALL} ${FLAGS}
            fi
          echo "::endgroup::"

      - name: Publish Hiero-Ledger NPM Package
        if: ${{ inputs.dual-publish-enabled == true && (needs.create-github-release.outputs.need-hl-publish == 'true' || inputs.dry-run-enabled == true) }}
        run: |
          echo "::group::Set Publish Args"
            FLAGS="--access=public"
            echo "flags=${FLAGS}"
          echo "::endgroup::"

          echo "::group::Set tarball output"
            TARBALL="./output/hl/${{ needs.get-package-info.outputs.hl-package-name }}"
            if [[ "${{ inputs.dry-run-enabled }}" == "true" ]]; then
              TARBALL=""
            fi
            echo "tarball=${TARBALL}"
          echo "::endgroup::"

          echo "::group::Publish Hiero-Ledger NPM Package"
            if [[ "${{ inputs.dry-run-enabled }}" != "true" ]]; then
              npm publish ${TARBALL} ${FLAGS}
            fi
          echo "::endgroup::"

  setup:
    name: Hugo Docs Setup
    runs-on: hiero-solo-linux-medium
    outputs:
      tag-ref: ${{ steps.set-inputs.outputs.tag_ref }}
    needs:
      - prepare-release
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@a90bcbc6539c36a85cdfeb73f7e2f433735f215b # v2.15.0
        with:
          egress-policy: audit

      - name: Set Inputs
        id: set-inputs
        run: |
          set -eo pipefail
          if [[ "${{ inputs.dry-run-enabled }}" == "true" ]]; then
            echo "Dry run enabled, setting ref to `github.ref` since tag won't exist."
            echo "tag_ref=${{ github.ref }}" >> $GITHUB_OUTPUT
            echo "tag_ref=${{ github.ref }}"
          else
            echo "Dry run not enabled, setting ref to 'refs/tags/v${{ needs.prepare-release.outputs.version }}'."
            echo "tag_ref=refs/tags/v${{ needs.prepare-release.outputs.version }}" >> $GITHUB_OUTPUT
            echo "tag_ref=refs/tags/v${{ needs.prepare-release.outputs.version }}"
          fi

  # Do a Hugo build for the docs site for the release branch
  hugo-docs-build:
    name: Hugo Docs Build
    uses: ./.github/workflows/zxc-hugo-build.yaml
    needs:
      - prepare-release
      - create-github-release
      - setup
    with:
      docs-build-label: 'v${{ needs.prepare-release.outputs.version }}'
      ref: ${{ needs.setup.outputs.tag-ref }}
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}

  # Do a Hugo build for the docs site for the main branch, required to ensure that docs for the main branch get deployed
  #  to GitHub Pages
  hugo-docs-build-main:
    name: Hugo Docs Build Main
    uses: ./.github/workflows/zxc-hugo-build.yaml
    needs:
      - hugo-docs-build
    with:
      docs-build-label: 'main'
      download-artifacts: true
      ref: 'main'
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}

  # Publish the Hugo docs site to GitHub Pages unless dry-run is enabled
  hugo-docs-publish:
    name: Hugo Docs Publish
    if: ${{ inputs.dry-run-enabled != true && !cancelled() && !failure() }}
    needs:
      - prepare-release
      - hugo-docs-build-main
    uses: ./.github/workflows/zxc-hugo-publish.yaml
    with:
      docs-build-label: 'v${{ needs.prepare-release.outputs.version }}'
      dry-run-enabled: ${{ inputs.dry-run-enabled }}
      attach-docs-enabled: true
