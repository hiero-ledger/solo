version: 0.1
merge:
  required_statuses:
    - "Code Style / Standard"
    - "Unit Tests / windows-2022"
    - "Unit Tests / hiero-solo-linux-medium"
    - "CodeCov / Standard"
    - "Codacy / Coverage"
    - "E2E Tests (Standard) / Standard"
    - "E2E Tests (Integration) / Integration"
    - "E2E Tests (Node Add Local) / Node Add Local"
    - "E2E Tests (Node Local PTT) / Node Local PTT"
    - "E2E Tests (Node Upgrade) / Node Upgrade"
    - "E2E Tests (Block Node) / Block Node"
    - "E2E Tests (One Shot Single) / One Shot Single"
    - "E2E Tests (External database new) / External database new"
    - "Hugo Docs Build / Build"
    - "Test Example (Address Book)"
    - "Test Example (Custom Network Config)"
    - "Test Example (External Database Test)"
    - "Test Example (Hardhat with Solo)"
    - "Test Example (Network with Block Node)"
    - "Test Example (Network with Domain Names)"
    - "Test Example (Node Create Transaction)"
    - "Test Example (Node Delete Transaction)"
    - "Test Example (Node Update Transaction)"
    - "Test Example (Local Build with Custom Config)"
    - "Test Example (Version Upgrade Test)"
    - "Test Example (One Shot Falcon)"
    - "Test Example (Rapid Fire)"
    - "Test Example (State Save and Restore)"
    - "Test Example (Backup Restore Workflow)"
    - "performance-test"
    - "Migrate Solo from Prior to Current Version"
    - "gcs-storage-test (aws_only)"
    - "gcs-storage-test (gcs_only)"
    - "gcs-storage-test (minio_only)"
